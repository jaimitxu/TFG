{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
<section class="vh-50">
	<div class="container py-5 h-100">
		<div class="row d-flex justify-content-center align-items-center h-100">
			<div class="col-12 col-md-8 col-lg-6 col-xl-5">
				<div class="card bg-dark text-white" style="border-radius: 1rem;">
					<div class="card-body p-5 text-center">
                        <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.name, {
            label: 'Nombre'
        }) }}
        {{ form_row(registrationForm.apellidos) }}
        {{ form_row(registrationForm.email, {
            label: 'Correo Electronico'
        }) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Contraseña'
        }) }}
        <a class="fs-7 fw-light" href="{{ path('app_terminos') }}">Mostrar terminos de servicio</a>
        {{ form_row(registrationForm.agreeTerms,{
            label: 'Confirmar los Terminos de Servicio'
        }) }}

        <button class="btn btn-outline-light btn-lg px-5 mt-5" type="submit">Register</button>
    {{ form_end(registrationForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>                    
{% endblock %}
