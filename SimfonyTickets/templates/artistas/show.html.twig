{% extends 'base.html.twig' %}

{% block title %}Artistas{% endblock %}

{% block body %}

	<h1><b>Artistas</b></h1>
					
        <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artista.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ artista.nombre }}</td>
            </tr>
            <tr>
                <th>GeneroMusical</th>
                <td>{{ artista.generoMusical }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ artista.descripcion }}</td>
            </tr>
            <tr>
                <th>Notas</th>
                <td>{{ artista.notas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_artistas_index') }}" class="btn btn-warning">back to list</a>

    <a href="{{ path('app_artistas_edit', {'id': artista.id}) }}" class="btn btn-success" ><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i>edit</a>

    {{ include('artistas/_delete_form.html.twig') }}
{% endblock %}
